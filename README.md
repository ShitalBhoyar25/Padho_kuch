# Padho_kuch

Git learning
18/02/2026
 ##Commands:
 1.git clone
 2.git status
 3.add: add new or changed filesin working directory  (git add <filename>)
 4.commit:changes in files or folders(git commit -m "some message")
 5.push-upload local repo content to remote repo (git push origin main)
    -origin :

 Status
 1.untracked:new file that donot know to git 
 2.modified:changed
 3:staged:file is ready to be commited(Add stage but not commit)
 4.unmodified:unchanges


for new repo push
Init command :
1.

Branches:
-
 
